# FACTS H1–H9 逐条核对报告（coder 任务6 ①）

- **核对人**：coder（Blair）
- **核对日期**：2026-09-05
- **对照物**：`01_OUTBOX/coder/solve.py`（main @ a1bcd54，632 行）↔ `00_CONTRACT/FACTS.md` v2.1 §五 假设清单
- **结论速览**：H1–H8 ✅ 一致；**H9 ❌ 定义与实现不一致**（需 modeler 改写，建议文案附后）。
- **说明**：FACTS.md 头部与 PR 闸均规定 `00_CONTRACT/` 仅 modeler 可改，故本报告不直接改动 FACTS.md，**请 modeler 依下表誊入打勾**（§五 表格三列均已备好）。

## 逐条核对

| 编号 | 结论 | 代码位置（solve.py） | 核对说明 |
|------|------|----------------------|----------|
| H1 | ✅ 一致 | `simulate()` L77–78；参数 `L_rail` L50（未参与积分） | t=0 状态直接取导轨出口 `s0=[0, h0, v0−v_e·sinβ, v_e·cosβ]`：人椅已在出口、初速=飞机速度矢量+沿导轨 v_e（向机尾偏 β）。导轨段不单独积分，`L_rail=1.05` 仅文档说明，与 FACTS §3.1 口径一致 |
| H2 | ✅ 一致 | `deriv()` L62–71；分段积分 `simulate()` L85–91 | 推力沿导轨方向恒定 T：`ax += −T·sinβ/m`（水平向机尾）、`ay += T·cosβ/m`（竖直向上）；`sol1` 积分 `(0, t1=5)` powered=True，`sol2` 积分 `(t1, 60)` powered=False；阻力与重力全程作用，与 H2 加速度表达式逐项对应 |
| H3 | ✅ 一致 | `KD` L55；`deriv()` L64–66 | 二次阻力 `a_drag = −(ρ·CdA/2m)·\|v\|·v` 全程作用（推力段同样存在）；ρ=1.225 常数。敏感性按 FACTS §3.2 做 v0/h0/β/m 四参数（不含 ρ），与 §3.2 一致 |
| H4 | ✅ 一致（附措辞建议） | `T_MAX` L53；`feasible()` L162–163；结果字段 `a_max_g` | 实现为 **火箭诱发过载 T/m ≤ 5g**（`T_MAX = a_lim·g·m = 8338.5 N`），与建模手开工指令"可行性必须卡 H4：T≤8339 N"及 results.json `a_max_g=3.7456`（=T*/mg）一致。⚠️ 建议模型手把 FACTS H4 的"合成加速度/g ≤ 5"微调为"火箭推力诱发的 +Gz 过载 ≤ 5g"——推力段气动阻力可达 ≈18g（−Gx 方向），若按字面"合成加速度"则任何推力都不可行；现有实现与全部既有决策口径一致，仅需文字消歧 |
| H5 | ✅ 一致 | `y_max_abs` L150；`feasible()` L169 | 轨迹最高**绝对海拔** `y_max_abs = max(y)`，判据 `y_max_abs < h_ox = 3000 m` |
| H6 | ✅ 一致 | 开伞触发 L101–108；裕量校验 L170 | 下降段（vy<0）首次 `\|v\| ≤ v_open=45` 触发，记录 `(t_open, y_open)`；`y_open ≥ h_margin=100 m` 入可行判定。伞后匀速段未显式积分——所有判据与输出（h_min=开伞高度、落地事件）只用开伞前轨迹+开伞点，伞后段无消费方，省略不影响结果（可在此注明为工程简化）。`v_open_max=97` 校验为隐式必然满足（触发时 \|v\|≤45<97）。`v_open=45 < v_term=58.3` 前提成立，且 P2 实测印证：低推力+低倾角时伞不触发→不可行（results.json P2 `safe` 判定） |
| H7 | ✅ 一致 | `constraints()` L119–127 | 飞机随体系：`s = v0·t − x`（人椅落后量，机尾为正）、`y_rel = y − h0`；垂尾窗口 `s∈[s_vt=7, L_plane=15]` 内取 `min(y_rel − h_vt=3.5)` = `vt_clear_min`，判 `> 0`（T_req 口径再加 0.5 m 余量，L54/L182–188） |
| H8 | ✅ 一致 | `constraints()` L129–138 | 尾喷流锥：`burn_clear_min = min( \|y_rel + dy_nozzle\| − (s − x_exh)·tan(α) )`，窗口 `s∈[9, 19]`（锥顶 (9, −0.5)、轴沿 +s、半角 12.5°、长 10 m），判 `> 0`。与 decisions-2026-09-04 §2.3 判定式逐项一致 |
| H9 | ❌ **不一致，需改写** | `solve_theta()` L262–297；`solve_p2()` L300–329 | **FACTS 原文**："Q2：飞机以倾角 θ 匀速爬升/下滑，地面系初速 = v0(cosθ, sinθ)"——此 θ 是**飞机飞行倾角**。**实际实现**（与建模手 PR #18 任务4 口径一致）：扫描的是**导轨后倾角 β**（`solve_theta` 中 `q["beta"] = θ`，L269），飞机仍水平匀速 v0，物理机制为"θ↑→推力竖直分量↓→推力下界↑"。results.json P2 全部字段（theta_sweep/θ*=7.5°）均基于此口径。**建议 H9 改写为**（见下节） |

## H9 建议改写文案（供 modeler 直接誊入）

> | H9 | Q2：以导轨后倾角 θ（与 β 同义，θ∈[0°,30°]）为优化变量逐点求解——飞机仍以 v0 水平匀速飞行（地面系初速 = (v0, 0)），每个 θ 下按 Q1 同一套约束与帕累托膝点法求推荐推力 T_opt(θ)，θ_best 取可行 θ 中 T_opt 最小者 | ⬜ 待 modeler 誊入 | coder | 2026-09-05 |

理由：① PR #18 任务4 行已按"后倾减小竖直分量"口径引用 θ*=7.5°，若 H9 改为"飞机爬升/下滑倾角"则 results.json 全部 P2 字段与论文 §5 口径互相矛盾；② 赛题 Q2 问的是"最优弹射方案"，导轨倾角是弹射装置可设计参数，比飞机飞行倾角更贴合"方案优化"语义（飞机倾角在弹射瞬间由作战态势决定，不是弹射系统可控变量）。

## 附：FACTS §3.3 输出结果表建议值（一并请 modeler 誊入）

| 键名 | 建议值 | 单位 | 来源 | 状态 |
|------|--------|------|------|------|
| `P1_T_min` | 2474.92（H7 余量 0.5 m 下界 T_req；推荐工作点为膝点 T*=6246.47） | N | coder/results.json P1 | 建议改 ✅ |
| `P1_h_min` | 1912.17（=h0+412.17，T* 工作点开伞前最低海拔） | m | coder/results.json P1 | 建议改 ✅ |
| `P1_a_max_g` | 3.7456（T* 工作点） | g | coder/results.json P1 | 建议改 ✅ |
| `P2_theta_best` | 7.5（T_opt=1491 N，h_min=1517.6 m） | ° | coder/results.json P2 | 建议改 ✅ |

## 复现自检（任务6 ②）✅ 通过

- **方式**：main @ a1bcd54 干净工作区（分支 `agent/coder/facts-verify`）重跑 `solve.py`，环境与首次产出一致（Python 3.13.12 + numpy 2.5.2 / scipy 1.18.1 / matplotlib 3.11.1）
- **对比对象**：git 已提交的 `01_OUTBOX/coder/results.json`（commit b73032e，PR #10 已合并）
- **运行耗时**：5 m 04 s
- **结果**：**✅ 完全复现**
  - `git diff` 除 `meta.generated_at` 时间戳外**零差异**（0 行）——T_req=2474.92 N、帕累托前沿 200 点、θ 扫描 13 点、敏感性全部数值逐位一致
  - 关键值复核：T_req=2474.92 N（建模手预演 2520 N ✓）、膝点 T*=6246.47 N、P1.h_min=1912.17 m、binding=H7、θ*=7.5°（T_opt=1491.1 N）
  - 顺带确认：fig4-1/5-1/6-1 与 table5-1/6-1 重新生成后 git 亦无差异（逐位复现）
- **结论**：results.json 的 `meta.reproducible: true` 声明成立，论文引用数值可放心以 main @ b73032e 版 results.json 为唯一真源
