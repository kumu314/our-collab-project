# writer 工作记录

代号：writer。负责章节 1–5 与代码附录的写作，不碰模型和求解。

## 一、本次认领

1. 章节 1–4 建模过程写作，依赖建模手给出的模型框架。
2. 章节 5 结果分析写作，依赖 coder 的 results.json。
3. 代码附录整理，依赖建模手的代码目录定稿。

## 二、写作纪律自查

1. 一切数字写成 `{{P1.total_cost}}` 这类占位符，不手抄建模手给的数。手抄一次，返工十次。
2. 图引用写「如图 3-2 所示」，不写文件名。
3. 不用「值得注意的是 / 不难看出 / 综上所述」这类套话。
4. 段落 3 到 5 句，长短交替，别每段一样长。
5. 长句拆短句，一句话只说一件事。

## 三、当前状态

第一次试跑。口径没定之前不动笔，避免写完再返工。等两件事：

1. 队长把 FACTS.md 的口径填出来。
2. 建模手把 results.json 的 schema 跑通，字段对齐 SPEC.md。

## 四、阻塞

1. FACTS.md 里客户总数、绿色配送区客户数还是空的，需要队长定口径。
2. 图题注编号规则要和建模手的出图命名对齐，否则正文引用对不上。
3. 章节 5 强依赖 results.json，建模手没出数前这一章是空转。
